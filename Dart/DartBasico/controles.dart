main() {
  // Método principal que é executado inicialmente
  var sexo = 'F';
  // No if o == é o igual
  if (sexo == 'F') {
    print('Executado o IF');
  }
  print('Finalizou');

  int idade = 18;
  // O || é igual ao "OU" d && é o "E"
  if (idade < 20 || idade > 50) {
    print('Encaixa');
  }
}